# FastAPI backend only (no Streamlit) — used by the Docker image that deploys
# to Hugging Face Spaces / Render / Cloud Run. The full-stack dev deps live in
# requirements.txt.
fastapi>=0.136
uvicorn>=0.48
python-multipart>=0.0.9

# Image I/O and processing
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# Background removal — withoutbg. Pinned so the HF model repo it fetches stays
# deterministic (1.0.6 uses withoutbg/withoutbg-openweights-onnx, which the
# Dockerfile pre-bakes; a future version could switch repos and break offline).
withoutbg==1.0.6
onnxruntime>=1.16.0
