# API backend (FastAPI; Next.js frontend lives in frontend/)
fastapi>=0.136
uvicorn>=0.48
python-multipart>=0.0.9

# Image I/O and processing
Pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# Background removal — withoutbg open-source model (Python API, no shell calls)
# Depth-Anything V2 + ISNet + focus matting/refiner. Weights download once from
# Hugging Face on first run and are cached locally. https://github.com/withoutbg/withoutbg
withoutbg>=1.0.3
onnxruntime>=1.16.0        # CPU inference backend for the ONNX models

# Testing 2 — OpenAI gpt-image-1 white-bg/studio-shadow generation.
# Needs OPENAI_API_KEY in the environment or a repo-root .env (see .env.example).
openai>=1.50.0
python-dotenv>=1.0.0
